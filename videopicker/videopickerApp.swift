import SwiftUI

// The main entry point of the SwiftUI app.
@main
struct videopickerApp: App {
    // Body property defining the main scene of the app.
    var body: some Scene {
        // WindowGroup represents the main window of the app.
        WindowGroup {
            // ContentView is the main view displayed in the main window.
            ContentView()
        }
    }
}
